// tsconfig.json controls how TypeScript compiles code.

// rootDir → where TS files exist  
// outDir → where JS files go  
// target → JS version  
// module → module system


//short practice code for typescript

const userName : string = "sumit";
const userAge: number = 20;

console.log(`my name is ${userName} and my age is ${userAge}`);

